# TypeScript library CI tests caller (Forgejo 15+).
name: CI Tests
on:
  push:
    branches: [main, "feature/**", "fix/**", "ci/**", "chore/**"]
  pull_request:
    branches: [main]
jobs:
  tests:
    uses: matteocervelli/adlimen-cicd/.forgejo/workflows/lib-ts-ci-test.yml@v2.2.14
    secrets: inherit
