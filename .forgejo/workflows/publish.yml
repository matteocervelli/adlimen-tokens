# TypeScript library NPM publish caller (Forgejo 15+).
name: NPM Publish
on:
  push:
    tags: ["v*.*.*"]
jobs:
  publish:
    uses: matteocervelli/adlimen-cicd/.forgejo/workflows/npm-publish.yml@v2.3.16
    secrets: inherit
