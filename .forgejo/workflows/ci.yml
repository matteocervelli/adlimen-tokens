# TypeScript library CI quality caller (Forgejo 15+).
name: CI Quality
on:
  push:
    branches: [main, "feature/**", "fix/**", "ci/**", "chore/**"]
  pull_request:
    branches: [main]
jobs:
  quality:
    uses: matteocervelli/adlimen-cicd/.forgejo/workflows/lib-ts-ci.yml@v2.2.14
    secrets: inherit
