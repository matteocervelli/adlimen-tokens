# Security scan caller — adlimen-tokens (Forgejo 15+).
name: CI Security
on:
  push:
    branches: [main, "feature/**", "fix/**"]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 5 * * 1"
jobs:
  security:
    uses: matteocervelli/adlimen-cicd/.forgejo/workflows/security.yml@v2.3.4
    secrets: inherit
